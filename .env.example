# ===============================================
# 사주 MBTI 프로젝트 환경변수 설정 예시
# ===============================================
# 이 파일을 복사하여 '.env' 파일로 저장한 후,
# 아래 값들을 실제 배포 환경에 맞게 수정하세요.
# ⚠️ .env 파일은 절대 GitHub에 커밋하지 마세요!

# ============== Django 보안 설정 ==============

# SECRET_KEY: Django 암호화 키 (필수)
# 배포 시에는 반드시 아래 명령어로 새로운 키를 생성하세요:
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=your-production-secret-key-here-change-this

# DEBUG 모드 (개발: True, 배포: False)
DJANGO_DEBUG=True

# 허용 호스트 (쉼표로 구분)
# 예: localhost,127.0.0.1,yourdomain.com
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# ============== 데이터베이스 설정 (선택) ==============
# SQLite 기본 사용 시 아래 설정은 불필요합니다.
# PostgreSQL 등 다른 DB 사용 시에만 추가하세요.

# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
